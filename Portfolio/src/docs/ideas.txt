There is a high chance that all the changes bellow have already been implemented lmao

-- Review the Spacing / figma and code
-- Blog needs new reader(add images to the right and put the article on the left)
-- Blog opening animation needs work
-- Make mobile design in figma then match in code / live
-- Think about making the p tag text slightly closer together
-- Align the svg in the footer a bit better
-- Custom menu / nav for mobile
-- Algin the process with vh units and other applicable locations / size from remains
-- Make the new opening animation
-- Make sure every image is using the subtle radius and accent overlay
-- Re do the backend part of the blog
-- Exlude certain parts of the project from the scroll blur
-- Favcon needs a update aka more zoomed in and the like
-- Better article images
-- Fix the who page personal details(the slight limitation and non alignment)
-- Review text
-- Document the animation's a bit better
-- Re organize the components folder
-- Disappear nav on client process pin